import React from "react";

const OwnerDashboard = () => {
  return (
    <div>
      <h2>Owner Dashboard</h2>
      <p>
        Shows relevant prices in various categories to help them set their own
        prices
      </p>
      <p>Make new listings and add them for all users to see</p>
    </div>
  );
};

export default OwnerDashboard;
